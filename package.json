{
  "name": "sls-dynamodb-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "IS_LOCAL=true npx nodemon --exec sls offline start --host 0.0.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.740.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "serverless": "^1.80.0",
    "serverless-dynamodb-local": "^0.2.39",
    "serverless-offline": "^6.6.0"
  }
}
